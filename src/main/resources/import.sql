create table movies (
       id int auto_increment,
       awardYear int,
       title varchar(255),
       studios varchar(255),
       producers varchar(255),
       winner varchar(255),
       primary key (id)
 );
 
 